# Docker Compose Environment Template
# Copy this to .env_postgres and update with your values

# Database Configuration
DB_TYPE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_secure_password
DB_NAME=csv_converter

# Logging
LOG_LEVEL=INFO